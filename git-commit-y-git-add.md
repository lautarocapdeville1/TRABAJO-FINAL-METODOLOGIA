Git commit y git add
git commit
El comando `git commit` guarda un momento de los cambios que agregaste con `git add` y permite comentar estos cambios hechos.

git add
el `git add` se utiliza para guardar los cambios y "prepararlos" para el git commit z